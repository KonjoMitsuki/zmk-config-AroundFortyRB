「次のルールに従って、指定した ZMK キーマップファイルから全レイヤーの“物理配列（現実のキーボード配置）”をMarkdownテーブルとして生成し、指定した出力ファイルに書き込んでください。

必須ルール：
1. ソースファイルを読み、定義されている全レイヤーを順に処理すること。
2. 各レイヤーについて、プログラム上のグリッド（bindings の行）を物理配置に並べ替えること（左右分割キーボードを想定して中心列を追加すること）。
3. テーブルの1行目は「指（小/薬/中/人/...）」のヘッダを入れること（例：小 薬 中 人 人 親 中心 親 人 人 中 薬 小）。
4. 記号キーはその記号で表記すること（例：`,` `.` `-` `+` 等）。
5. `&lt`/`&mt` 等の関数タイプは名前や変数で表記すること（例：`lt(8, MINUS)`、`mt_a_ctrl(LCTRL, A)`）。
6. 長押しと単押しで挙動が変わるキーは「**長押し**,単押し」の形式で記載すること。長押しで入力されるもの（ホールドで出るキー）を太字（Markdown の `**`）にすること。例：`**LCTRL**,A`。
7. レイヤー番号やコード表記はインラインコードで囲むこと（例：`` `8` ``）。
8. transparent は `trans` とする。
9. 中心を示す列は `中心` と記載し、視認しやすくすること。
10. 出力は可読性を優先した Markdown テーブルで、各レイヤーごとに見出し（例：`### レイヤー: Win-Base (layer 0)`）を付けること。
11. 生成後、出力ファイルの差分を表示するための簡単な確認行（「生成済み」など）を追記しておくこと。

追加オプション（指定があれば適用）：
- 特定のレイヤーのみを出力する（例: `--layers Win-Base,Mac-Base`）。
- 既存の同名テーブルを置換する（デフォルトは置換）。

出力例（1行のみ抜粋）：
| 小 | 薬 | 中 | 人 | 人 | 親 | 中心 | 親 | 人 | 人 | 中 | 薬 | 小 |
| --- | --- | --- | --- | --- | --- | :--: | --- | --- | --- | --- | --- | --- |
| **LCTRL**,A | S | D | F | G |  | 中心 |  | H | J | K | L | **`8`**,MINUS |

実行時の注意：
- ソースのキー名（`JP_LBRC` 等）は読み替えず表記するか、必要なら展開ルールを別途指定してください。
- 出力ファイルは上書きされるため、必要なら事前にバックアップを取ってください。

ここまでの指示に従って、まずは `Win-Base` をサンプル生成してください。完了したら残り全レイヤーを同様に出力してください。

